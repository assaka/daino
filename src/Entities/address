{
  "name": "Address",
  "type": "object",
  "properties": {
    "user_id": {
      "type": "string"
    },
    "type": {
      "type": "string",
      "enum": ["shipping", "billing", "both"],
      "description": "Address type: shipping, billing, or both"
    },
    "full_name": {
      "type": "string"
    },
    "street": {
      "type": "string"
    },
    "city": {
      "type": "string"
    },
    "state": {
      "type": "string"
    },
    "postal_code": {
      "type": "string"
    },
    "country": {
      "type": "string"
    },
    "phone": {
      "type": "string"
    },
    "email": {
      "type": "string"
    }
  },
  "required": [
    "user_id",
    "full_name",
    "street",
    "city",
    "postal_code",
    "country"
  ]
}